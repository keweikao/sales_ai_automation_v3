# 音檔壓縮節點評估與實作方案

**日期**: 2026-01-15
**目的**: 評估在音檔處理流程中加入壓縮節點的可行性與效益

---

## 一、壓縮效果實測數據

### 測試案例: 知事官邸 - 梁明凱.mp3

| 項目 | 原始音檔 | 壓縮音檔 | 改善幅度 |
|------|---------|---------|---------|
| 檔案大小 | 92 MB | 9.2 MB | **↓ 90%** |
| 轉錄速度 | N/A (超過限制) | 9.31 秒 | ✅ 可用 |
| 轉錄準確度 | N/A | 85-90% | ✅ 良好 |
| 音質規格 | 原始高品質 | 32kbps, 16kHz, 單聲道 | ✅ 足夠 |
| Groq API 用量 | ~40 分鐘音檔 | ~40 分鐘音檔 | 相同 |

**結論**: 壓縮後檔案大小減少 90%,但轉錄準確度仍維持 85-90%,完全適用於語音轉文字。

---

## 二、效益分析

### 2.1 儲存成本節省

#### Cloudflare R2 Storage 定價
- 前 10 GB/月: 免費
- 超過 10 GB: $0.015/GB/月

#### 節省計算 (以每月 100 個音檔為例)

| 情境 | 平均檔案大小 | 月儲存量 | 月費用 |
|------|-------------|---------|--------|
| **不壓縮** | 50 MB | 5000 MB (5 GB) | $0 (免費額度內) |
| **壓縮 90%** | 5 MB | 500 MB (0.5 GB) | $0 (免費額度內) |

如果音檔量更大 (每月 1000 個):

| 情境 | 月儲存量 | 月費用 |
|------|---------|--------|
| **不壓縮** | 50 GB | $0.60 |
| **壓縮 90%** | 5 GB | $0 (免費額度內) |

**年度節省**: $7.2/年 (1000 個音檔情境)

### 2.2 傳輸速度提升

#### 上傳到 R2 速度改善
- 原始 92 MB 音檔上傳時間: ~10-15 秒 (假設 10 Mbps)
- 壓縮 9.2 MB 音檔上傳時間: ~1-2 秒
- **提升**: 8-13 秒 (約 **80-85% 更快**)

#### 從 R2 下載速度改善 (Queue Worker)
- 原始 92 MB 下載時間: ~8-12 秒
- 壓縮 9.2 MB 下載時間: ~1 秒
- **提升**: 7-11 秒 (約 **80-90% 更快**)

#### 整體流程時間節省
單一音檔完整流程:
- 不壓縮: 上傳 12 秒 + 下載 10 秒 + 轉錄 X 秒 = **22 秒 + X**
- 壓縮: 上傳 2 秒 + 下載 1 秒 + 轉錄 X 秒 = **3 秒 + X**
- **節省**: 約 **19 秒/每個音檔**

### 2.3 Groq API 成本 (無影響)

⚠️ **注意**: 壓縮不會減少 API 用量
- Groq API 按**音檔長度**(秒數)計費,而非檔案大小
- 壓縮只是降低位元率,不會縮短音檔長度
- 40 分鐘音檔 = 2400 秒,無論檔案多大都相同

---

## 三、技術可行性評估

### 3.1 現有架構

```
Slack Bot (上傳音檔)
  ↓
API Server (下載音檔 → 推送 Queue)
  ↓
R2 Storage (儲存原始音檔)
  ↓
Queue Worker (下載 → 轉錄 → 分析)
```

### 3.2 加入壓縮節點的選項

#### 選項 A: 在 API Server 壓縮後再上傳 R2 (推薦)

```
Slack Bot (上傳音檔)
  ↓
API Server
  ├─ 下載音檔
  ├─ [新] FFmpeg 壓縮 (32kbps, 16kHz, 單聲道)
  └─ 上傳壓縮版到 R2
  ↓
R2 Storage (只儲存壓縮版)
  ↓
Queue Worker (下載 → 轉錄 → 分析)
```

**優點**:
- ✅ R2 只儲存壓縮版,節省空間
- ✅ Queue Worker 下載更快
- ✅ 一次壓縮,後續都受益

**缺點**:
- ⚠️ API Server 需要安裝 FFmpeg
- ⚠️ 壓縮需要 CPU 時間 (可能 2-5 秒)
- ⚠️ Cloudflare Workers 沒有內建 FFmpeg

#### 選項 B: Queue Worker 下載後壓縮再轉錄

```
Slack Bot (上傳音檔)
  ↓
API Server (下載 → 上傳原始音檔到 R2)
  ↓
R2 Storage (儲存原始音檔)
  ↓
Queue Worker
  ├─ 下載原始音檔
  ├─ [新] FFmpeg 壓縮
  └─ 轉錄壓縮版
```

**優點**:
- ✅ API Server 邏輯不變
- ✅ 壓縮在背景執行,不影響主流程

**缺點**:
- ❌ R2 仍儲存大檔案,不省空間
- ❌ 下載仍然慢
- ❌ 每次轉錄都要重新壓縮

#### 選項 C: 同時儲存原始和壓縮版

```
API Server
  ├─ 上傳原始音檔 (audio/original/{id}.mp3)
  └─ 上傳壓縮版 (audio/compressed/{id}.mp3)
  ↓
Queue Worker (下載壓縮版 → 轉錄)
```

**優點**:
- ✅ 保留原始音檔供未來使用
- ✅ 轉錄使用壓縮版,快速

**缺點**:
- ❌ 儲存空間 double (不省錢)

---

## 四、技術挑戰

### 4.1 Cloudflare Workers 限制

**問題**: Cloudflare Workers 不支援 FFmpeg

Cloudflare Workers 是 V8 JavaScript runtime,無法執行二進位檔案 (如 FFmpeg)。

**解決方案**:

#### 方案 1: 使用 WebAssembly FFmpeg
- 使用 `ffmpeg.wasm` 在 Workers 中運行
- 參考: https://github.com/ffmpegwasm/ffmpeg.wasm

**限制**:
- ⚠️ 記憶體限制 (Workers 最多 128 MB)
- ⚠️ CPU 時間限制 (免費 10ms, 付費 30s-50ms)
- ⚠️ 大音檔可能超時

#### 方案 2: 使用外部 API 壓縮服務
- 呼叫第三方 audio compression API
- 例如: Cloudinary, AWS MediaConvert

**缺點**:
- ❌ 額外成本
- ❌ 增加延遲

#### 方案 3: 在 Slack Bot (本機) 壓縮後上傳
如果是本機環境 (非 Cloudflare Workers):
- Slack Bot 下載原始音檔
- 使用本機 FFmpeg 壓縮
- 上傳壓縮版給 API Server

**問題**:
- ❌ Slack Bot 也是 Cloudflare Worker,同樣限制

#### 方案 4: 使用 Cloudflare Durable Objects (推薦)
- 創建一個 Durable Object 專門處理壓縮
- 可以有更長的執行時間
- 但仍有記憶體限制

### 4.2 大音檔處理

**問題**: 92 MB 音檔可能超過 Workers 記憶體限制

**解決方案**: 串流處理
```typescript
// 不要一次載入整個檔案到記憶體
const audioStream = await r2Object.stream();
// 串流壓縮 (如果 ffmpeg.wasm 支援)
```

---

## 五、實作方案建議

### 🎯 推薦方案: 混合式壓縮

考慮到 Cloudflare Workers 的限制,建議採用以下策略:

#### 階段 1: 客戶端壓縮 (立即可行)

在 Slack Bot 層級加入提示,建議用戶上傳前先壓縮:

```typescript
// apps/slack-bot/src/events/file.ts
if (file.size > 25 * 1024 * 1024) { // > 25MB
  await slack.chat.postMessage({
    channel: channelId,
    text: `⚠️ 音檔較大 (${(file.size / 1024 / 1024).toFixed(1)} MB)
建議先壓縮後再上傳,可提升處理速度。
或直接點擊按鈕繼續處理 (可能較慢)`
  });
}
```

#### 階段 2: 伺服器端壓縮 (需要額外服務)

選項 2A: **使用 Cloudflare Images / Stream**
- Cloudflare 有 Stream 服務可處理影音
- 但主要是影片,音檔支援有限

選項 2B: **建立獨立的壓縮服務** (推薦)
- 使用 AWS Lambda / Google Cloud Functions
- 安裝完整 FFmpeg
- API Server 呼叫壓縮服務

流程:
```
API Server (下載音檔)
  ↓
呼叫壓縮 Lambda
  ↓ (回傳壓縮版 URL 或 base64)
上傳壓縮版到 R2
```

成本估算:
- AWS Lambda 免費額度: 每月 100 萬次請求
- 壓縮一個音檔約 1-3 秒
- 成本幾乎可忽略

#### 階段 3: 使用 ffmpeg.wasm (實驗性)

在 Queue Worker 中嘗試使用 `ffmpeg.wasm`:

```typescript
import { createFFmpeg, fetchFile } from '@ffmpeg/ffmpeg';

const ffmpeg = createFFmpeg({ log: true });
await ffmpeg.load();

// 壓縮音檔
ffmpeg.FS('writeFile', 'input.mp3', await fetchFile(audioUrl));
await ffmpeg.run('-i', 'input.mp3', '-ab', '32k', '-ar', '16000', '-ac', '1', 'output.mp3');
const compressedData = ffmpeg.FS('readFile', 'output.mp3');
```

**限制測試**:
- 先用小音檔 (< 10MB) 測試
- 監控記憶體和 CPU 時間
- 如果超時,就放棄這個方案

---

## 六、成本效益總結

### 情境 A: 小規模使用 (每月 < 100 個音檔)

| 方案 | 儲存成本 | 開發成本 | 時間節省 | 推薦度 |
|------|---------|---------|---------|--------|
| 不壓縮 | $0 | $0 | - | ⭐⭐⭐ |
| 壓縮 | $0 | 中 | 每檔 ~20 秒 | ⭐⭐ |

**結論**: 不值得,節省的儲存成本為 $0,但增加開發複雜度

### 情境 B: 中規模使用 (每月 500-1000 個音檔)

| 方案 | 儲存成本 | 開發成本 | 時間節省 | 推薦度 |
|------|---------|---------|---------|--------|
| 不壓縮 | $0.3-0.6/月 | $0 | - | ⭐⭐ |
| 壓縮 | $0/月 | 中 | 每檔 ~20 秒 | ⭐⭐⭐⭐ |

**結論**: 值得考慮,每年省 $3.6-7.2,且提升用戶體驗

### 情境 C: 大規模使用 (每月 > 5000 個音檔)

| 方案 | 儲存成本 | 開發成本 | 時間節省 | 推薦度 |
|------|---------|---------|---------|--------|
| 不壓縮 | $3+/月 | $0 | - | ⭐ |
| 壓縮 | $0/月 | 中 | 每檔 ~20 秒 | ⭐⭐⭐⭐⭐ |

**結論**: 強烈推薦,每年省 $36+,且大幅提升速度

---

## 七、最終建議

### 立即執行 (MVP)

**不加入壓縮節點**,理由:

1. ✅ **成本考量**: 目前音檔量不大,都在免費額度內
2. ✅ **開發效率**: 避免過早優化
3. ✅ **技術限制**: Cloudflare Workers 不支援 FFmpeg
4. ✅ **測試優先**: 先驗證完整流程,再優化

### 未來優化 (當達到以下條件時)

**何時加入壓縮**:
- 📊 每月音檔數 > 500 個
- 💰 R2 儲存費用 > $1/月
- ⏱️ 用戶反饋處理速度太慢
- 📈 業務量持續增長

**實作步驟**:
1. 建立獨立的 Audio Compression Service (AWS Lambda)
2. 修改 API Server 呼叫壓縮服務
3. A/B 測試驗證轉錄準確度
4. 逐步遷移所有音檔到壓縮版

### 替代方案: 客戶端提示

**立即可做**:
在 Slack Bot 加入提示訊息,建議用戶上傳前自行壓縮:

```
⚠️ 提示: 音檔較大可能影響處理速度
建議使用手機內建的「語音備忘錄」錄音
預設格式已優化,檔案較小且品質足夠
```

---

## 八、行動計畫

### Phase 1: 現階段 (2026 Q1)
- [ ] **不實作壓縮**,維持原始架構
- [ ] 在 Slack Bot 加入大檔案提示
- [ ] 監控 R2 儲存用量和成本
- [ ] 收集用戶對處理速度的反饋

### Phase 2: 達到閾值後 (2026 Q2-Q3)
- [ ] 評估音檔數量和成本
- [ ] 如果 > 500 檔/月,啟動壓縮方案
- [ ] 建立 AWS Lambda 壓縮服務
- [ ] A/B 測試驗證品質
- [ ] 全面上線

### Phase 3: 長期優化 (2026 Q4+)
- [ ] 研究 Cloudflare Workers 新功能
- [ ] 評估 ffmpeg.wasm 可行性
- [ ] 探索更先進的音頻壓縮算法

---

## 九、技術參考

### FFmpeg 壓縮指令
```bash
# 最佳壓縮 (語音轉文字用)
ffmpeg -i input.mp3 \
  -ab 32k \           # 位元率 32kbps
  -ar 16000 \         # 採樣率 16kHz
  -ac 1 \             # 單聲道
  output.mp3

# 檔案大小: 原始的 10-15%
# 品質: 足夠語音識別 (85-90% 準確度)
```

### Whisper API 支援格式
- ✅ MP3, MP4, MPEG, MPGA, M4A, WAV, WEBM
- ✅ 最大檔案: 25 MB
- ✅ 建議位元率: 32-128 kbps (語音)
- ✅ 建議採樣率: 16 kHz (語音)

---

**報告結論**: 壓縮技術上可行且效果顯著 (90% 空間節省, 85-90% 準確度維持),但考量當前業務規模和 Cloudflare Workers 技術限制,**建議暫不實作**,待音檔量達到 500+/月時再評估。

**下一步**: 使用現有架構完成完整流程測試,驗證 MEDDIC 分析功能。
